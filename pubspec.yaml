name: flutterqiweibao
description: A new Flutter module.

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  logger: ^1.0.0
  dio: ^4.0.6
  image_picker: ^0.8.5+3
  flutter_easyloading: ^3.0.3
  fluttertoast: ^8.0.9
  photo_view: ^0.13.0
  pull_to_refresh: ^2.0.0
#  shared_preferences: ^2.0.15
#  barcode_scan: ^3.0.1
#  keyboard_utils: ^1.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_lints: ^1.0.0


flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: com.example.flutterqiweibao
    iosBundleIdentifier: com.example.flutterqiweibao
